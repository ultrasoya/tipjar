import styles from './styles.module.css';

const Header = () => (
    <header className={styles.header}>
        <h1>Crypto Tip Jar</h1>
        <p>Make a donation and watch the magic happen</p>
    </header>
);

export default Header;